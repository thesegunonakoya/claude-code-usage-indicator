# Contributing

Bug reports, PRs, and feature requests are welcome.

- Test on both X11 and Wayland before submitting a GNOME-side PR
- The statusline script must pass shellcheck with no errors
- Keep the GNOME extension dependency-free (no npm, no bundler)
- Open an issue before large changes so we can align on direction
